FROM quay.io/minio/minio:latest

# The bucket service stays private on Railway; the application connects over
# Railway's private network using the injected MINIO_ROOT_* credentials.
CMD ["server", "/data", "--address", ":9000", "--console-address", ":9001"]
